node_modules/
dist/
*.local
.DS_Store

# Secrets — never commit
.env
.env.*
!.env.example
.vercel

# Track the shipped browser model (overrides the root *.onnx ignore)
!public/models/chronael.int8.onnx

# Wrangler local dev artifacts
.wrangler/
